# Upsun configuration, generated using AI at: 2026-01-29T01:48:49Z
# AI can make mistakes. Please modify this file to suit your needs.
applications:
  backend:
    type: nodejs:24

    build:
      flavor: none

    hooks:
      build: |
        set -ex
        npm install --omit=dev

    web:
      commands:
        start: npm start

      locations:
        /:
          passthru: true
          allow: false

        /api:
          passthru: true

        /health:
          passthru: true

    relationships:
      database: {}

services:
  database:
    type: mariadb:11.8

routes:
  https://{default}/:
    type: upstream
    upstream: backend:http